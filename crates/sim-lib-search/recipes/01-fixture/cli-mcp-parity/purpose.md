# CLI/MCP search parity

Run `sim search research sim --offline --json`, or call the projected
`search/research` Card through MCP. Both surfaces return the canonical
`search/Run` Shape record. Grant `net/http` separately for live operation;
`show` only reads an existing stable id and never revalidates it.
