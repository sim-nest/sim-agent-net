# Replay a bounded federated research run

This all-fixture specimen freezes a plan, records provider claims and partial
failure, clusters only exact or configured aliases, fuses with shared RRF,
captures selected pages under independent authority, verifies selectors, and
replays the resulting research bundle without network access.
