# sim-lib-search

`sim-lib-search` composes Retriever skills into immutable, bounded search plans,
deterministic partial runs, conservative alias clusters, shared RRF ranking,
independently authorized captures, and replayable research bundles. Provider
claims remain visibly distinct from captured observations; this crate does not
generate prose answers.

See the fixture-only recipe and crate tests for a network-free research run.
