[package]
name = "sim-lib-provider"
version = "0.1.0"
edition = "2024"
license = "MPL-2.0"
description = "Reusable provider identity, discovery, and seat-opening contracts for SIM."
readme = "README.md"
publish = true
repository = "https://github.com/sim-nest/sim-agent-net"
homepage = "https://github.com/sim-nest/sim-agent-net"

[lib]
path = "src/lib.rs"

[dependencies]
sim-citizen = { version = "0.2.0" }
sim-citizen-derive = { version = "0.2.0" }
sim-cookbook = { version = "0.2.0" }
sim-kernel = { version = "0.2.0" }
sim-lib-agent-runner-core = { version = "0.2.0" }

[build-dependencies]
sim-cookbook-build = { version = "0.1.0" }

[lints]
workspace = true
