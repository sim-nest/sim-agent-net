# Open provider seat

Shows the stable printable identity used to select one seat from an open,
runtime-discovered provider family. Concrete adapters open the selected seat as
the shared `ModelRunner` execution seam.
