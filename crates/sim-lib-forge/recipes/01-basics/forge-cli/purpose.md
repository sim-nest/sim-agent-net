# Forge command flow

Runs the local command story for a compiled intent: lift the prose, review the
return shape and data-degrade surface, promote the artifact, run it, and show the
same request answered from replay with no execution model call.
