# Compiled intent record

Documents the stable descriptor that names a lifted prose intent, points at its normalized source and BRIDGE packet by content id, and keeps human approval distinct from automated verification. The recipe is a network-free descriptor fixture.
