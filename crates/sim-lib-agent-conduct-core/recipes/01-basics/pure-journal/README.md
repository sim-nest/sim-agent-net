# Pure conduct journal

This recipe constructs the standard run frame, appends two redacted events,
and verifies the canonical hash chain. It performs no model, tool, graph, host,
storage, or network operation.
