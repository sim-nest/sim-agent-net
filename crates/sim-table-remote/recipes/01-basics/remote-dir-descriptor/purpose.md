# Remote directory descriptor

Documents how `sim-table-remote` exposes a remote directory as ordinary SIM
table operations over an eval fabric site. The descriptor records the operation
surface and required authority without contacting a remote table backend from
the cookbook sandbox.
