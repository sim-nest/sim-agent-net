# Bounded OAuth discovery over the shared HTTP organ

`sim-lib-oauth-http` makes every metadata/JWK byte cap, redirect decision,
refresh interval, and stale-key generation visible. It accepts only HTTPS
discovery targets and composes the constellation's one HTTP policy boundary.
