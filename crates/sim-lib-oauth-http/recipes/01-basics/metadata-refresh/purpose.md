# Retrieve bounded authority documents

Fetch OAuth metadata and keys through the shared HTTP organ with explicit caps,
redirect policy, and refresh time.

This is a sandbox descriptor because discovery requires an operator-selected
authority and the shared HTTP effect. Crate tests execute the policy against
injected bounded responses without granting cookbook network access.
