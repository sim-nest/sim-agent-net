# Explicit legacy composition

This specimen records that initialize-era behavior is selected at process
construction. Modern traffic cannot auto-upgrade the connection into a legacy
session after dispatch has begun.
