# Provider-neutral fixture search site

This fixture composes a small `SearchWireCodec` with the bounded HTTP skill
transport. It demonstrates that provider wire syntax remains outside the
transport while live capture and cassette replay produce the same `SearchPage`.
