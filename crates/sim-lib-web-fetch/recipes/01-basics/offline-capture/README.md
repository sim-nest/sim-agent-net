# Offline immutable capture

The production API accepts injected transport, egress policy, and caller-owned
capture storage. The executable fixture summarizes the crate test that scripts
robots plus a landing response, records immutable raw and normalized ids,
replays by raw id with no further transport calls, and proves an extractor
version change creates a new representation over unchanged raw evidence.
