# Immutable web evidence, not ambient browsing

Capture a landing page through explicit egress, robots, redirect, media, rate,
and research-budget decisions. Keep the raw exchange and every normalized
representation content-addressed in caller-owned storage, then replay it
offline without touching DNS or a socket. Model-facing text is fenced to the
exact evidence identity, so hostile page text remains data.
