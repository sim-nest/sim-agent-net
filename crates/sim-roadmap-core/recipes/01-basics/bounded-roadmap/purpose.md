# Admit bounded roadmap intent

Keep authored intent, reviewed implementation guidance, promises, and limits in
one immutable revision. Execution and completion remain separate authorities.

This is a sandbox descriptor because admission is a typed roadmap-core API, not
a loadable cookbook operation. Focused crate tests construct revisions and
exercise all admission limits.
