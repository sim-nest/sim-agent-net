(quote
  (provider openai
    (config
      (endpoint "https://api.openai.com/v1")
      (model "gpt-5-mini")
      (api-key-env "OPENAI_API_KEY")
      (probe (mode manual)))
    (probe provider/probe)
    (use runner/openai)))
