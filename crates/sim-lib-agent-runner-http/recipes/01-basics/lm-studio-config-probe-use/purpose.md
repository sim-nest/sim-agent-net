# LM Studio config, probe, and use (descriptor)

Documents the inert provider config, model-list probe, and runner use for LM Studio.
The endpoint is loopback by default; optional bearer auth is named through an
environment variable rather than a committed secret. The HTTP calls are
documented rather than run in the cookbook sandbox.
