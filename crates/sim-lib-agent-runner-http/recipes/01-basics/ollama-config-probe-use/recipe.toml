id = "ollama-config-probe-use"
title = "Ollama config, probe, and use"
codec = "lisp"
setup = "setup.siml"
purpose = "purpose.md"
order = 40
tags = ["agent", "runner", "http", "provider", "ollama", "sandbox-descriptor"]
requires = ["agent-runner-http", "codec/lisp"]
