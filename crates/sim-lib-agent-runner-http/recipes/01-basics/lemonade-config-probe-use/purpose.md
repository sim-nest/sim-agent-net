# Lemonade config, probe, and use (descriptor)

Documents the inert provider config, model-list probe, and runner use for Lemonade.
The endpoint is loopback by default and the descriptor contains no secret field.
The HTTP calls are documented rather than run in the cookbook sandbox.
