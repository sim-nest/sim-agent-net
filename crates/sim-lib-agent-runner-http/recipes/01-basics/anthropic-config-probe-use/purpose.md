# Anthropic config, probe, and use (descriptor)

Documents the inert provider config, model-list probe, and runner use for Anthropic.
The recipe names the secret environment variable but contains no secret value.
The HTTP calls are documented rather than run in the cookbook sandbox.
