id = "transport-port-descriptor"
title = "Describe transport ports"
summary = "Keep sockets, DNS, and IPC behind host-injected ports instead of ambient process access."
codec = "lisp"
setup = "setup.siml"
purpose = "purpose.md"
expected = "expected.txt"
order = 10
tags = ["transport", "ports", "sandbox-descriptor"]
requires = ["transport"]

[[expect]]
form = 0
result = "(transport-ports (authority host-injected) (socket port) (dns port) (ipc optional))"
