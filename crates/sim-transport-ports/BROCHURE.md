# Transport ports

Run unchanged SIM protocols over deterministic modeled networks or an explicitly bound platform capsule. Script fragmentation, loss, pressure, resolution failures, collisions, cancellation, and half-close without touching the host.
