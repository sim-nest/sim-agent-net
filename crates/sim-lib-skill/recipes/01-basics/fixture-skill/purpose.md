# Skill card (descriptor)

Documents a skill card fixture: a transported operation (here a `math` add) exposed as an
invokable skill. The skill is invoked through the agent transport outside the cookbook sandbox
eval stack, so the card is documented rather than invoked live.
