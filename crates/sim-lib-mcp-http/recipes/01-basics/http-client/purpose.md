# Call final-protocol MCP

Authorization values are marked sensitive, redirects and ambient credentials
remain governed by the supplied net-http policy, and streaming frames are
decoded only at complete SSE message boundaries.
