# Content-addressed fleet (descriptor)

Documents a fleet as a content-addressed store of nodes (not a bespoke router). Placement and
content-addressed lookup run through the eval fabric outside the cookbook sandbox eval stack, so
the fleet model is documented rather than run.
