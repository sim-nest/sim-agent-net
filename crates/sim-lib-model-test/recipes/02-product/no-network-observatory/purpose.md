# Replay the model observatory offline

This specimen records the complete provider-free product path: fixture census,
pack selection, smoke and screen evidence, interruption and resume,
confirmation, replay, role decisions, portfolio disposition, pick, and
deterministic export. The crate's `product` integration test executes the same
boundary and proves that read-only projections make no provider contact and an
export imported into an empty evidence store is byte-identical.
