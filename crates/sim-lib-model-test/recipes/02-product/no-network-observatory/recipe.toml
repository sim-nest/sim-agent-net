schema = "sim.recipe/v1"
id = "no-network-observatory"
title = "Replay the complete model observatory without provider contact"
kind = "model-test-conformance"
entrypoint = "setup.siml"

[evidence]
command = "cargo test -p sim-lib-model-test --test product"
