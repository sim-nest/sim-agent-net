# JSON stdio fixture

Process runners are live resources, so cookbook data should describe their
protocol instead of launching one. This recipe captures a JSON stdio request
and response fixture with an explicit no-spawn marker.
