# Pure roadmap operation specimens

The checked crate tests are the runnable source for these six specimens:

1. A bounded source deck retains every evidence witness and limitation.
2. A grounded guide binds caller-owned source content to an admitted roadmap.
3. A three-level proposal yields one certified successor only from its exact base.
4. A v3 import separates observations from semantic roadmap identity.
5. An exact diff is empty for equal semantic values and names semantic change otherwise.
6. A recursive roadmap sent to flat-v3 rendering returns every lossy projection path.

None of the specimens opens a file, executes work, acquires a capability, or edits
the input. See `src/tests.rs` for the executable assertions.
