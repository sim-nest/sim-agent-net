# Inspect promise discharge

Use `sim roadmap explain` and `status` for one execution identity. These routes
read typed proof and discharge values; they cannot mint completion or edit a
roadmap status.
