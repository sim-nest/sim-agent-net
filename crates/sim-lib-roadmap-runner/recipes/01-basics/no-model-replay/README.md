# Replay without a model

Run `sim roadmap replay` with explicit `--execution`, `--conduct`,
`--model-pick`, `--proof-catalog`, and `--runner-generation` content identities.
The result is a journal-only replay receipt and performs no model, mutation, or
delivery effect.
