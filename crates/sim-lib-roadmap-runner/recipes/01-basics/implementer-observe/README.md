# Observe a recorded implementer

Use `sim roadmap run --observe` with the five explicit content identities. The
receipt repeats all identities and acknowledges the journal without requesting
checkout mutation authority.
