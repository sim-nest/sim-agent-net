This recipe records the thin agent-tool projection over the general CORE host
primitives. Each tool names the same capability as the underlying table, edit,
find, process, or HTTP operation, so the agent view adds no extra authority.
