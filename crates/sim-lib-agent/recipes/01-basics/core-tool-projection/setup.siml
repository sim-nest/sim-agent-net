(quote
  (agent-core-tools
    (tool read  (requires fs/read))
    (tool write (requires fs/write))
    (tool list  (requires fs/read))
    (tool edit  (requires fs/read edit))
    (tool find  (requires fs/read find))
    (tool exec  (requires exec))
    (tool fetch (requires net/http))))
