(quote (code-generation-descriptor (id a30-013-code-generation) (task add-boundary-check) (failing-fixture (test rejects-empty-name) (message missing-validation)) (proposed-change (file virtual/customer-form.rs) (operation insert-guard) (guard name-not-empty) (effect return-validation-error)) (test-plan (step compile-virtual) (step run rejects-empty-name) (step run accepts-trimmed-name)) (sandbox (capability sandbox) (fs writes-denied) (network denied) (command cargo-test-virtual) (limit max-steps 3)) (check-result (mode descriptor-only) (status pass) (tests rejects-empty-name accepts-trimmed-name)) (effect-ledger (effect propose-patch descriptor) (effect request-sandbox-check allowed) (effect deny-real-file-write enforced))))
