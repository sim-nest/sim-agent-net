# phased conduct

This offline specimen uses a fake runner, cassette replies, fixture tools, and modeled effects. The complete package is in `conduct.simtopo`; change only the `topology agent/phased-v1` line to select another agent kind.
