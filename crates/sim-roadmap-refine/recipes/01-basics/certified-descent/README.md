# Certify roadmap descent

This recipe compares two already-derived work profiles and reports the exact
ordinal component that proves the child is strictly smaller. Production code
derives profiles from admitted roadmap and grounding values before comparison.
