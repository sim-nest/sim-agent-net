# BRIEF instruction packet

This recipe is a descriptor for a BRIDGE request that uses the BRIEF profile. The
packet carries typed frame records, while the runtime renders the model-facing
sentence from registered frame templates and keeps every byte covered by an
ownership span.
