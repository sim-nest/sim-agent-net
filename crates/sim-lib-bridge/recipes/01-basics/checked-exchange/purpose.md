# Checked exchange descriptor

This recipe is a descriptor for the BRIDGE runtime guard. A live exchange needs a
model placement and a packet book, which the cookbook sandbox does not install.
The descriptor names the surface that a host uses when it wants the same packet
checks on both send and receive.
