# LOOM validated weave

This recipe describes a LOOM packet whose rows choose heads from the shared
frontier and bind roles only to context or earlier row slots. The checker derives
the result shape from those rows before the packet can leave SIM.
