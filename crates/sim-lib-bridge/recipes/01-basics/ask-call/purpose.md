# ASK call packet

This recipe describes an ASK packet that carries a typed model call, fenced call
arguments, and a declared return contract. The packet can be checked before it
leaves SIM and checked again when the model returns data.
