# Explainable roadmap plans

Turn authored intent plus exact source witnesses into a deterministic,
inspectable plan. Every ready phase, blocker, invalidation, promise, and
completion decision traces to bounded facts rather than scheduler state.
