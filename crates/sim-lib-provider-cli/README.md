# sim-lib-provider-cli

Loadable `sim provider` command adapter over `sim-lib-provider`. It parses
operator intent and renders redaction-safe provider inventory; provider
registry, authentication, probing, opening, and fan-out remain owned by the
provider organ and its installed adapters.
