[package]
name = "sim-lib-provider-cli"
version = "0.1.1"
edition.workspace = true
license.workspace = true
description = "Loadable provider command surface for the SIM bootloader."
readme = "README.md"
publish = true
repository.workspace = true
homepage.workspace = true

[dependencies]
sim-kernel = { version = "0.4.0" }
sim-lib-provider = { version = "0.2.0", path = "../sim-lib-provider" }
sim-run-core = { version = "0.5.0" }

[lints]
workspace = true
