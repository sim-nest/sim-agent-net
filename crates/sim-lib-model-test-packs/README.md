# sim-lib-model-test-packs

Frozen model-test task packs built only from pinned Git objects. The crate owns
public manifests, immutable fixture epochs, exact selection revisions, pack
validation, and the private-loader interface. It does not own study execution
and never discovers private control-plane files.
