# Frozen model-test packs

Grow model-test task data without changing an engine. Each public pack binds
licenses, toolchain, lockfile, complete source closure, graders, work bounds,
and exact smoke-through-reproduction selections to an immutable epoch rebuilt
from Git objects. Private task bytes stay behind a control-plane loader and
cannot enter public registry or export records.
