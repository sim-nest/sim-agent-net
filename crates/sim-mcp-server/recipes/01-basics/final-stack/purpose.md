# Final MCP product combinations

This bounded descriptor is the runnable discovery entry point for the final
stdio pair, authenticated Streamable HTTP pair, explicit legacy probe,
protected MRTR, concurrent subscription, cancellation, caller-supplied shared
Table, and custom extension examples. Each name routes to the same typed MCP
contracts used by the product and conformance matrix; it defines no protocol.
